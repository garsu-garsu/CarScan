package com.bruni.carscan.core.units

/**
 * Units are configured per quantity, never as a single metric/imperial switch:
 * the UK uses miles, litres and imperial MPG at the same time.
 */
enum class SpeedUnit { KM_PER_HOUR, MILES_PER_HOUR }
